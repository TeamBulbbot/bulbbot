const SendLog = require("../handlers/SendLog");
const Emotes = require("../emotes.json");

module.exports = async (_client, _guild, _user) => {
  //SendLog.Mod_action(client, guild.id, `${Emotes.actions.ban} Banned **${user.username}**#${user.discriminator} \`\`(${user.id})\`\` by **Manual ban**`, "");
};
