const SendLog = require("../handlers/SendLog");
const Emotes = require("../emotes.json");

module.exports = async (client, guild, user) => {
  //SendLog.Mod_action(client, guild.id, `${Emotes.actions.unban} Unbanned **${user.username}**#${user.discriminator} \`\`(${user.id})\`\` by **Manual Unban**`, "");
};
