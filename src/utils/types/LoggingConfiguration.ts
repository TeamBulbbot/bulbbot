export interface LoggingConfiguration {
	id: number;
	modAction: string;
	automod: string;
	message: string;
	role: string;
	member: string;
	channel: string;
	joinLeave: string;
	createdAt: string;
	updatedAt: string;
	other: string;
}
