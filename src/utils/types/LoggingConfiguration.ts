export default interface LoggingConfiguration {
	id: number;
	modAction: string;
	banpool: string;
	automod: string;
	message: string;
	role: string;
	member: string;
	channel: string;
	thread: string;
	invite: string;
	joinLeave: string;
	createdAt: string;
	updatedAt: string;
	other: string;
}
