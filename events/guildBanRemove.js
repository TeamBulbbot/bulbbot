const SendLog = require("../handlers/SendLog");

module.exports = async (client, guild, user) => {
	//SendLog.Mod_action(client, guild.id, `Unbanned **${user.username}**#${user.discriminator} \`\`(${user.id})\`\` by **Manual Unban**`, "");
};
